# Python tooling artefacts (this repo is a Copier template; the build-system
# exists only so `pip install .[test]` can pull the pytest deps)
uv.lock
.venv/
__pycache__/
*.py[co]
*.egg-info/
.pytest_cache/
build/
dist/

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Test renders
/tmp/
